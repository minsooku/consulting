import 'package:flutter/material.dart';

class MissionPickerContent extends StatelessWidget {
  const MissionPickerContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Mission Picker – coming soon')),
    );
  }
}
