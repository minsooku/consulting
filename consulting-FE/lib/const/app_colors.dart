import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFCFCFD);

  // Main (모던 블랙 & 화이트)
  static const Color mainPoint = Color(0xFF000000);
  static const Color mainPointText = Color(0xFFFFFFFF);

  // Sub (테두리, 비활성)
  static const Color sub = Color(0xFFE5E5EA);

  // Accent (포인트 블루 - iOS 스타일)
  static const Color accent = Color(0xFF007AFF);

  // Text
  static const Color textPrimary = Color(0xFF111111); // 완전 쌩블랙보단 살짝 부드러운 블랙
  static const Color textSecondary = Color(0xFF6E6E73);

  // Status
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFF9500);
  static const Color danger = Color(0xFFFF3B30);
}
